<?php
	//include autoloader
	/*require_once'dompdf-master/autoload.inc.php';
	// reference the dompdf namespace
	use Dompdf\Dompdf;
	$dompdf=new Dompdf();
	$dompdf->loadHtml("<h1 style=\"color=red;\">voila mon premier pdf réaliser en php<h1>");
	$dompdf->set_paper('A4','landscape');
	//Render the HTML as PDF
	$dompdf->render();
	//output the generated pdf
	$dompdf->stream('test', array('Attachement' =>1));*/
	echo "bonjour tout le monde ";
?>